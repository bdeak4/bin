#!/usr/bin/env bash

repo=$1
project_dir=$1
github_user=bartol

[ -z $repo ] && echo "Must supply repository name" && exit

mkdir -p ~/dev
cd ~/dev
git clone git@srv1:/srv/git/$repo

cd $repo
git remote add github git@github.com:$github_user/$repo
